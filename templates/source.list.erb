deb <%= scope.lookupvar('url') -%> <%= scope.lookupvar('dists').join(',') -%> <%= scope.lookupvar('sections').join(',') %>
<% if scope.lookupvar('deb_src') -%>
deb <%= scope.lookupvar('url') -%> <%= scope.lookupvar('dists').join(',') -%> <%= scope.lookupvar('sections').join(',') %>
<% end -%>
